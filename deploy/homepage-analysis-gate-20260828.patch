diff --git a/public/index.html b/public/index.html
index f2e88f3..793009e 100644
--- a/public/index.html
+++ b/public/index.html
@@ -1585,13 +1585,16 @@ function liveHomeLogo(m){return m&&(m.home_logo||m.homeLogo||m.home_badge||m.str
 function liveAwayLogo(m){return m&&(m.away_logo||m.awayLogo||m.away_badge||m.strAwayTeamBadge||(m.teamB&&m.teamB.logo))||'';}
 function liveHomeScore(m){var v=m&&(m.score_home!=null?m.score_home:m.home_score!=null?m.home_score:m.intHomeScore!=null?m.intHomeScore:m.goals&&(m.goals.home!=null?m.goals.home:null));return v==null?'':v;}
 function liveAwayScore(m){var v=m&&(m.score_away!=null?m.score_away:m.away_score!=null?m.away_score:m.intAwayScore!=null?m.intAwayScore:m.goals&&(m.goals.away!=null?m.goals.away:null));return v==null?'':v;}
+function tlmHomepageAnalyzedMatch(m){
+  return !!(m&&tlmMatchAllowed(m)&&m.client_product_eligible===true&&m.analysis_verified===true&&m.homepage_display_eligible===true);
+}
 async function loadLive(){
   var grid=document.getElementById('live-rows');
   try{
     var r=await fetch('/api/live-matches');
     var d=await r.json();
-    var matches=(d.matches||d.live||[]).filter(tlmMatchAllowed).slice(0,3);
-    if(!matches.length){ grid.innerHTML='<div class="empty-note">Aucun match suivi n\'est actuellement en direct.<br>Consulte les prochaines rencontres disponibles.</div>'; return; }
+    var matches=(d.matches||d.live||[]).filter(tlmHomepageAnalyzedMatch).slice(0,3);
+    if(!matches.length){ grid.innerHTML='<div class="empty-note">Aucun signal n\'a encore été validé par au moins 4 IA sur 5.<br>Le moteur continue d\'analyser les championnats autorisés.</div>'; return; }
     grid.innerHTML=matches.map(function(m){
       var icon=SPORT_ICO[m.sport]||'🏟️';
       var rawHomeScore=liveHomeScore(m),rawAwayScore=liveAwayScore(m),sh=rawHomeScore===''?'-':rawHomeScore,sa=rawAwayScore===''?'-':rawAwayScore;
@@ -1679,19 +1682,19 @@ function renderHeroWatchlist(p){
 async function loadHeroLive(){
   try{
     var r=await fetch('/api/live-matches?t='+Date.now(),{cache:'no-store'}),d=await r.json();
-    var matches=(d.matches||d.live||[]).filter(tlmMatchAllowed);
+    var matches=(d.matches||d.live||[]).filter(tlmHomepageAnalyzedMatch);
     matches.sort(function(a,b){
       function quality(m){return (liveHomeLogo(m)?2:0)+(liveAwayLogo(m)?2:0)+(liveHomeScore(m)!==''?2:0)+(liveAwayScore(m)!==''?2:0)+Math.min(1,Number(m.minute||0)/100);}
       return quality(b)-quality(a);
     });
     tlmHasLiveMatch=!!matches[0];
-    if(tlmHasLiveMatch)renderHeroLive(matches[0]);else renderHeroWatchlist(tlmFeaturedMatch);
-  }catch(e){tlmHasLiveMatch=false;renderHeroWatchlist(tlmFeaturedMatch);}
+    if(tlmHasLiveMatch)renderHeroLive(matches[0]);else renderHeroLive(null);
+  }catch(e){tlmHasLiveMatch=false;renderHeroLive(null);}
 }
 loadHeroLive();setInterval(loadHeroLive,30000);
 
 /* Met a jour le service worker meme si l'accueil precedent etait reste en cache. */
-if('serviceWorker' in navigator){window.addEventListener('load',function(){navigator.serviceWorker.register('/sw.js?v=tlm-app-v5-20260828').catch(function(){});});}
+if('serviceWorker' in navigator){window.addEventListener('load',function(){navigator.serviceWorker.register('/sw.js?v=tlm-app-v6-20260828').catch(function(){});});}
 
 /* ══ MATCHS A VENIR (pre-match, H2H) ══ */
 function renderUpcomingItem(p){
@@ -2282,4 +2285,4 @@ document.addEventListener('click', function(e){
 })();
 </script>
 </body>
-</html>
\ No newline at end of file
+</html>
diff --git a/public/sw.js b/public/sw.js
index 1c147e1..818cc21 100644
--- a/public/sw.js
+++ b/public/sw.js
@@ -3,7 +3,7 @@
  * Le cache reste uniquement un secours hors ligne.
  */
 
-const VERSION = "tlm-app-v5-history-beta-20260828";
+const VERSION = "tlm-app-v6-home-analysis-gate-20260828";
 const SHELL_CACHE = `${VERSION}-shell`;
 const DATA_CACHE = `${VERSION}-data`;
 
@@ -128,4 +128,4 @@ self.addEventListener("fetch", (event) => {
     }
     return response;
   })));
-});
\ No newline at end of file
+});
diff --git a/scripts/api_server.js b/scripts/api_server.js
index c511b3c..1a04bb7 100644
--- a/scripts/api_server.js
+++ b/scripts/api_server.js
@@ -11946,9 +11946,23 @@ app.get("/live-matches", async (req, res) => {
     // message parlant de minutes de football. Une seule source de vérité ici.
     const withVerdict = matches.map((m) => {
       const ou25 = getLiveOu25VoteState(m);
-      if (m.pinnedSignal) return { ...m, analysable: false, block_reason: null, ou25 };
+      const clientProductEligible = isClientOu25MatchEligible(m, true);
+      const alignedVotes = Math.max(Number(ou25.over_count || 0), Number(ou25.under_count || 0));
+      // Source de verite pour l'accueil public : un match ne peut etre presente
+      // comme un signal que si le championnat est dans le perimetre client ET
+      // qu'au moins 4 IA ont reellement enregistre le meme vote O/U 2,5.
+      // Cela evite qu'un simple match live bien illustre (logos + score) soit
+      // affiche avec un faux statut "Analyse IA en cours" alors qu'il est a 0/5.
+      const homepageDisplayEligible = clientProductEligible && alignedVotes >= CLIENT_OU25_MIN_VOTES;
+      const visibility = {
+        client_product_eligible: clientProductEligible,
+        analysis_started: Number(ou25.vote_count || 0) > 0,
+        analysis_verified: homepageDisplayEligible,
+        homepage_display_eligible: homepageDisplayEligible,
+      };
+      if (m.pinnedSignal) return { ...m, analysable: false, block_reason: null, ou25, ...visibility };
       const reason = livePickBlockReason(m);
-      return { ...m, analysable: !reason, block_reason: reason, ou25 };
+      return { ...m, analysable: !reason, block_reason: reason, ou25, ...visibility };
     });
 
     // Règle du 29/07/2026 ("n'afficher que ce qui est jouable") assouplie le
diff --git a/scripts/test_homepage_analysis_gate.js b/scripts/test_homepage_analysis_gate.js
new file mode 100644
index 0000000..c079af3
--- /dev/null
+++ b/scripts/test_homepage_analysis_gate.js
@@ -0,0 +1,41 @@
+#!/usr/bin/env node
+"use strict";
+
+const fs = require("fs");
+const path = require("path");
+
+const root = path.resolve(__dirname, "..");
+const api = fs.readFileSync(path.join(root, "scripts", "api_server.js"), "utf8");
+const home = fs.readFileSync(path.join(root, "public", "index.html"), "utf8");
+const serviceWorker = fs.readFileSync(path.join(root, "public", "sw.js"), "utf8");
+
+function assert(condition, message) {
+  if (!condition) throw new Error(message);
+}
+
+assert(
+  api.includes("const homepageDisplayEligible = clientProductEligible && alignedVotes >= CLIENT_OU25_MIN_VOTES"),
+  "Le serveur doit exiger une ligue client eligible et au moins 4 votes alignes."
+);
+assert(
+  api.includes("homepage_display_eligible: homepageDisplayEligible"),
+  "L'API doit exposer la decision d'affichage issue du serveur."
+);
+assert(
+  home.includes("function tlmHomepageAnalyzedMatch(m)"),
+  "L'accueil doit posseder un garde-fou explicite."
+);
+assert(
+  home.includes(".filter(tlmHomepageAnalyzedMatch)"),
+  "Le hero et la liste live doivent filtrer les matchs non analyses."
+);
+assert(
+  !home.includes("var matches=(d.matches||d.live||[]).filter(tlmMatchAllowed).slice(0,3)"),
+  "La liste d'accueil ne doit plus se contenter du filtre visuel historique."
+);
+assert(
+  home.includes("/sw.js?v=tlm-app-v6-20260828") && serviceWorker.includes("tlm-app-v6-home-analysis-gate-20260828"),
+  "Le cache PWA doit changer de version avec le garde-fou d'accueil."
+);
+
+console.log("OK homepage_analysis_gate");
