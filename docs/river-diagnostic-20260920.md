# Exceptional River Plate diagnostic

User-authorized one-off analysis of fixture 1493147. Fresh live feed required. Five real model calls under the existing spend guard and a USD 0.10 reservation bound. Limited score-only assessment, no fabricated stats or odds. Abstention/invalid outputs remain unavailable.

No official predictions, settlements, signal snapshots or Telegram publications are written. Separate timestamped HTML report, opt-in text panel on homepage/app/live-ia using `?test=river`. This respects CSP without iframe changes. This does not prove automatic signal generation. Production HTML backed up before idempotent insertion; rollback restores those pages. HTTP verification follows the confirmed 301 redirect to www.

Validation: diagnostic behavioral test and provider quota regression tests passed. `npm test -- --watchAll=false` exits 1 because no React tests are present, not treated as a passing suite.
